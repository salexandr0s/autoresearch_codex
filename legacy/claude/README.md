# Legacy Claude compatibility

This repository is now Codex-first.

A legacy Claude bundle is **not** shipped in this repo snapshot. If compatibility assets are later restored, keep them under `legacy/claude/` and treat them as compatibility-only, never as the source of truth.
